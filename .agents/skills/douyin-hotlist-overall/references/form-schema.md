# Form Schema

本文件描述 `douyin-hotlist-overall` 技能的输入参数规范。

## 参数列表

本技能**无输入参数**。用户无需提供任何参数，直接触发即可获取抖音实时热搜榜单。

## 完整 Schema（JSON Schema 格式）

```json
{
  "type": "object",
  "properties": {}
}
```

## 说明

- `douyin-hotlist-overall` 是一个无参数技能
- 用户直接询问"现在最热门的是什么"等类似问题即可激活
- API 返回抖音全网实时热搜榜单数据
